name: CI/CD is Easy!

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: My first step
        uses: actions/checkout@main
        with:
          node-version: '16'
      - run: npm ci
      - run: npm run build

      
